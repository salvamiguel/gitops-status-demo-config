apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-status-demo
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: gitops-status-demo
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
