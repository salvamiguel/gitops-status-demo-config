apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: gitops-status-demo
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: gitops-status-demo
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
